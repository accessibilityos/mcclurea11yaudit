{% assign has_header_mast = false %}

{% include 'html-user-nav' %}

<div id="template">
  	<div class="row text-center">
      <div class="small-12 large-4 large-centered columns">
        <div class="template_header">
          <h1 class="title" id="page_title" style="margin-top: 20px">Reset Account Password</h1>
          <p class="note">Enter a new password for {{ email }}</p>
        </div>
      </div>
  </div>
    <div class="row">
      <div class="small-12 loginBox large-4 large-centered columns  text-center">

    {% form 'reset_customer_password' %}
      {{ form.errors | default_errors }}

      <div id="password" class="clearfix large_form">
        <label class="large" for="password">Password</label>
        <input type="password" value="" name="customer[password]" id="customer_password" class="large password" size="16" />
      </div>
      <div id="password_confirm" class="clearfix large_form">
        <label class="large" for="password_confirmation">Password Confirmation</label>
        <input type="password" value="" name="customer[password_confirmation]" id="customer_password_confirmation" class="large password" size="16" />
      </div>
      <div class="action_bottom">
        <input class="btn btn_def" type="submit" value="Reset Password" />
      </div>
    {% endform %}
      </div>
  </div>
</div>
